import { PercentilePipe } from './percentile.pipe';

describe('PercentilePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentilePipe();
    expect(pipe).toBeTruthy();
  });
});
